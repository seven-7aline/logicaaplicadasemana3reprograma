// Crie uma função que recebe o ano de nascimento da pessoa informando se ela é maior de idade ou menor.

function informaMaiorOuMenorDeIdade(numero) {
    const media = (nota1 + nota2 + nota3) / 3
    
    if(media >= 7 && media <= 10) {
        return `Aprovada, sua média é ${media}`
    } else if (media >= 5 && media < 7) {
        return `Recuperação, sua média é ${media}`
    } else if(media < 5 && media >= 0) {
        return `Reprovada, sua média é ${media}`
    }
    }
    console.log(resultadoBoletim(9,8,7,8))
    